# Toy Redis client in Go
Learning project not to be used in production environment.

## Test
First start a redis server by running `docker-compose up` in test directory.
Then run tests with `go test`
